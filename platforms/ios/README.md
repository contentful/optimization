# Optimization iOS SDK

iOS SDK support is planned for a future pre-release phase and is not yet implemented in this
repository.

## Current Status

- A native Swift iOS SDK layer is planned.
- No iOS platform package is published from this monorepo yet.
- Current mobile support is available through the JavaScript React Native SDK:
  [@contentful/optimization-react-native](../javascript/react-native/README.md).
