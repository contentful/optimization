Apply these rules to TypeScript, TSX, and TypeScript examples in documentation. A finding must survive
the relevant evidence check below. If the evidence is unavailable or ambiguous, do not post it.

UNUSED DECLARATIONS
- Search the whole repository and the complete containing file before calling a declaration unused.
- Count type-only references, indexed access such as `Callbacks['handler']`, overload support, and
  declaration-only variance helpers as real uses. Type-only code emitting no JavaScript is not dead
  merely because it has no runtime reference.

MODULE CONTEXT
- Before claiming that `await` lacks an async context, inspect the complete file or fenced example.
  Static `import` or `export` makes the code an ES module, where top-level await is valid when the
  documented target supports it. Flag only a demonstrated non-module or unsupported-target use.

MOCK RESTORATION
- Distinguish standalone `fn()` mocks from `spyOn()` or stubs that replace properties on shared
  objects, prototypes, modules, or globals.
- Require restoration only when shared state was mutated and can survive the test. A test-local
  standalone mock passed into a test-local fixture does not require `mockRestore()`.

PUBLIC EXPORTS
- Do not infer that a type needs a package-level export merely because its source declaration uses
  `export`. First verify the package's configured entrypoints and show a consumer-facing API that
  requires callers to name the type.
- Internal helper types used to construct an exported declaration do not need separate public
  exports. Do not recommend expanding the public API for convenience or symmetry alone.

PRIVATE STATE AND DERIVED OUTPUTS
- A private field does not need a same-named property on a public snapshot, event, or return type.
  Trace how it derives the existing public fields and identify a documented public contract or real
  consumer that requires the raw value before recommending API expansion.
- Do not claim consumers read an absent private field as `undefined` when the public type does not
  declare that access. Check whether the raw state is already available through its owning public
  SDK surface.

CONSTRUCTOR INITIALIZATION
- A method call before a field assignment is not evidence of an uninitialized read. Trace the full
  constructor call graph, including short-circuit branches, and identify the exact access to that
  field before reporting a failure.

TEST GLOBALS
- Before reporting a missing test import, inspect the test runner's `globals` configuration and the
  package TypeScript `types`. A configured global used successfully by package typecheck and the
  focused test is not a missing import.

GENERICS AND OVERLOADS
- Read every generic default before claiming omitted arguments become `unknown`, lose inference, or
  break compatibility.
- Distinguish public overloads from their implementation signature. The implementation signature is
  not caller-visible; an overload matching it can intentionally expose a catch-all union contract.
- Do not add generics to sibling APIs for consistency when the type is only consumed internally and
  does not appear in a return value or callback.

CONTENTFUL ENTRY GENERICS
- For `OptimizedEntry` and bound Next.js `OptimizedEntry` components, generic `S` is an
  `EntrySkeletonType`, not an `Entry<S>`. The render prop receives the resolved Contentful
  `Entry<S, ...>`.
- `<OptimizedEntry<MyEntrySkeleton>>` is the expected form. Replacing the skeleton argument with
  `Entry<MyEntrySkeleton>` is incorrect.
- A successful implementation TypeScript typecheck disproves a claimed generic mismatch unless the
  finding identifies a specific unsound cast or untyped boundary.

TYPE NARROWING AND VALIDATION
- A switch covering every member of a closed discriminated union does not need a default branch.
  Flag it only when a declared union member is unhandled.
- Runtime validation for external data belongs at the response boundary. Do not suggest a default
  throw inside an otherwise fail-soft resolution or rendering path as a substitute for validation.
- A type assertion scoped to one destructured property does not discard runtime data or weaken
  unused sibling properties. Flag an assertion only when it can let an invalid value cross a real
  runtime or public API boundary.

BRANCH-SPECIFIC DATA FLOW
- Trace each branch of a discriminated union independently before declaring nested and top-level
  fields duplicates. Verify which representation each branch consumes.
- When a descriptor, option, or source variant changes, follow every behavior-affecting field
  through normalization, batching, cache or in-flight keys, serialization, handoff, hydration,
  aliases, and downstream consumption. Flag any stage that drops a field or derives identity from
  different effective inputs than the operation it represents.

TEST AND API INTENT
- Before replacing `toBe` with `toEqual`, determine whether the test intentionally asserts reference
  preservation. Do not weaken an identity contract into value equality.
- Two methods returning the same type are not duplicates when one is a documented compatibility
  alias or convenience name. Verify history and callers before recommending removal or renaming.

PIPELINE OWNERSHIP
- Do not report a type error that the repository's TypeScript typecheck would necessarily catch. If
  the PR's typecheck passes and no unsound boundary or missing compile-time contract is demonstrated,
  skip the finding.

ASYNC ERROR OWNERSHIP
- An awaited promise is not an unhandled rejection. Report an unhandled rejection only when the
  promise is floating, detached, or crosses a terminal boundary without a caller or framework owning
  error propagation.
- Do not require local catch-and-fallback behavior unless an existing contract defines that fallback.
  Propagating a rejection can be the intended error behavior.

TRUST BOUNDARIES
- Before requesting runtime validation, trace the value to its producer and identify the external or
  attacker-controlled path.
- Do not request duplicate validation for values produced and sanitized internally or when the
  consuming platform primitive already enforces the invariant.
