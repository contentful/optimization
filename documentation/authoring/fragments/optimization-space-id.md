---
fragment: optimization-space-id
---

## Context

Mandatory in `## Before you start`, as a nested clarification under the **Your Contentful space
values** bullet, placed immediately after the "for your Contentful space" sentence.
sentence. Copy the Template verbatim. It documents the exact navigation path a first-time integrator
uses to find the space ID and environment. The parent bullet keeps its SDK-specific tail (API base
URL defaults) after the inline path.

## Template

Find them in the Contentful web app under **Apps → Installed apps → Contentful Personalization →
SDK keys**.
