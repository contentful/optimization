---
fragment: optimization-client-id
---

## Context

Mandatory in `## Before you start`, as a nested clarification under the **Your Optimization
project values** bullet, placed immediately after the "from your Optimization project settings."
sentence. Copy the Template verbatim. It documents the exact navigation path a first-time integrator
uses to find the client ID and environment. The parent bullet keeps its SDK-specific tail (API base
URL defaults) after the inline path.

## Template

Find them in the Contentful web app under **Apps → Installed apps → Contentful Personalization →
SDK keys**.
